server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # SPA: unknown paths fall back to index.html so the Angular router handles them
  location / {
    try_files $uri $uri/ /index.html;
  }
}
